const express = require('express');
const router = express.Router();
const cartsCtrl = require('../../controllers/api/carts');

// router.get('/', cartsCtrl.getAll);
// router.post('/create', cartsCtrl.create);

module.exports = router;
